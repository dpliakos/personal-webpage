/*
    A placeholder component for placing all css presets and default settings
    for the project.
*/

import React from "react";
import "./css-presets.scss";

const CSSPresets = () => <></>;

export default CSSPresets;
